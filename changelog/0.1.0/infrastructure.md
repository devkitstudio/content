---
title: Infrastructure
order: 4
icon: server
---

### Deployment CLI

- Docker-based deployment with `devkit` CLI
- Nginx reverse proxy with SSL support
- Selective Git submodule initialization
- `devkit build` / `devkit deploy` / `devkit init` commands
